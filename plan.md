## Покроковий план впровадження архітектури **Golos 2.0 (Satellite)**.

---

### 📅 Етап 1: Підготовка та Фундамент (The Engine Room)

**Мета:** Створити "Вкладку-двигун", яка вміє відкриватися у фоні, не засинати і слухати мікрофон. UI поки не чіпаємо.

1.  **Рефакторинг файлової системи:**
    - Створити папки: `engine/`, `content/`, `background/`, `assets/`.
    - Рознести поточні файли (або створити пусті заготовки) згідно з новою структурою.
2.  **Оновлення `manifest.json`:**
    - Додати нові права: `tabs` (щоб керувати вкладкою-двигуном), `scripting`.
    - Переписати шляхи до `service_worker` та `content_scripts`.
3.  **Розробка `engine.html` та `engine.js`:**
    - Це має бути проста сторінка, яка запускає `webkitSpeechRecognition`.
    - Реалізація логіки `Keep-Alive` (щоб вкладка не "вмирала").
4.  **Налаштування `background/service-worker.js`:**
    - Написати функцію `ensureEngineTab()`: при старті розширення перевіряти, чи відкритий двигун. Якщо ні — відкривати його (pinned/inactive).

✅ **Критерій готовності Етапу 1:**
Ти встановлюєш розширення -> У браузері автоматично відкривається (і закріплюється) вкладка `Golos Engine`. В консолі цієї вкладки видно: "Engine ready".

---

### 📅 Етап 2: Обличчя та Інтеграція (The Widget & Wiring)

**Мета:** Щоб на будь-якому сайті з'являлася наша модалка (віджет), і вона отримувала текст від двигуна.

1.  **Розробка `content/dom-injector.js` (Shadow DOM):**
    - Написати клас `GolosWidget`.
    - Створити HTML/CSS для плаваючої панелі (мікрофон, статус, текстове поле).
    - Ізолювати стилі, щоб сайт не ламав наш віджет.
2.  **Налаштування маршрутизації повідомлень (The Router):**
    - Навчити `background.js` пересилати команди:
      - `UI` -> `Background` -> `Engine` (Старт/Стоп).
      - `Engine` -> `Background` -> `UI` (Текст/Помилки).
3.  **Зв'язка "Гаряча клавіша":**
    - При натисканні `Alt+Shift+G` -> показуємо віджет -> шлемо команду "Старт" у двигун.

✅ **Критерій готовності Етапу 2:**
Ти натискаєш `Alt+Shift+G` на Facebook -> з'являється наша панелька. Ти говориш -> у панельці біжать літери в реальному часі.

---

### 📅 Етап 3: Вставка тексту та Логіка завершення (Execution)

**Мета:** Щоб текст потрапляв у поле вводу, і все працювало стабільно.

1.  **Розробка `content/input-simulator.js`:**
    - Написати функцію безпечної вставки тексту.
    - Додати підтримку `input` подій (для React/Vue сайтів, щоб вони "бачили" вставку).
2.  **Логіка завершення сесії:**
    - Обробка команди `FINAL_TRANSCRIPT` від двигуна.
    - Вставка тексту -> приховування віджета.
3.  **Auto-Timeout (Детектор тиші):**
    - В `engine.js` додати таймер: якщо 2-3 секунди немає результатів -> автоматично відправляти фінальний текст і зупиняти запис.

✅ **Критерій готовності Етапу 3:**
Ти диктуєш речення, замовкаєш. Через 2 секунди віджет блимає зеленим, текст вставляється в поле, віджет зникає.

---

### 📅 Етап 4: Полірування (UX Polish)

**Мета:** Зробити це схожим на Voice In (звуки, іконки, комфорт).

1.  **Індикація в браузері:**
    - Змінювати іконку розширення (сіра -> червона `ON`).
    - Додати бейдж "ON".
2.  **Звуковий зворотний зв'язок (SFX):**
    - Додати короткий "біп" при старті запису і "чпок" при завершенні. (Звуки гратиме `engine.js`).
3.  **Обробка помилок:**
    - Що, якщо мікрофон заблоковано?
    - Що, якщо інтернету немає?
    - Показувати красиві повідомлення у віджеті.
4.  **Збереження налаштувань:**
    - Вибір мови у Popup має миттєво змінювати мову розпізнавання в Engine.
